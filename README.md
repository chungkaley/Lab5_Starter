# Lab 5 - Starter
Kaley Chung

Make sure you make a PR to your own repo's main and not the class' repo!! Otherwise you will lose points!!

1. I would not use a unit test to test the “message” feature of a messaging application. This is because a unit test tests one piece of application code. The “message” feature that allows a user to write and send a message to another user depends on multiple components, such as the UI, server, and database, working together. 
 
2. I would use a unit test to test the “max message length” feature of a messaging application. This is because  the “max message length” is an isolated piece of application code. The feature checks whether the message exceeds 80 characters, so it does not depend on any other components.


